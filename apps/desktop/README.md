# `@your-flare-mails/desktop`

Tauri 2 shell wrapping the built `apps/web` output for macOS, Windows, and Linux
(and later iOS/Android in Phase 10).

Scaffolding is deferred to **Phase 9**. Do not initialize a Tauri project here until
that phase.
