/**
 * Default theme tokens (colors, typography, spacing).
 * Independent of packages/core — Phase 4 adds the real token system.
 */
export const PACKAGE_NAME = '@your-flare-mails/theme' as const;

export type PackageName = typeof PACKAGE_NAME;
