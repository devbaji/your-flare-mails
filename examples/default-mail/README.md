# `examples/default-mail`

Minimal example that composes `@your-flare-mails/core` and
`@your-flare-mails/server` **without** `packages/ui` or `packages/theme`.

This proves the framework APIs are sufficient to build a custom mailbox UI.
Full verification lands in **Phase 12**.
